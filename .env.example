# Database related options
POSTGRES_HOST = "localhost"
POSTGRES_PORT = 5432
POSTGRES_DB = "highlight"
POSTGRES_USERNAME = "postgres"
POSTGRES_PASSWORD = "root"

# Discord token for the bot application
DISCORD_TOKEN="owo"

# Ignore anything after this
POSTGRES_URL = "postgresql://${POSTGRES_USERNAME}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"

